data:
  seq_len: 128
  # Input Dimensions (Must match your Vectorizer/Dataset)
  logs_emb_dim: 32
  text_emb_dim: 64
  cve_emb_dim: 16 
training:
  batch_size: 16
  lr: 0.001
  weight_decay: 0.00001
  max_epochs: 5
model:
  d_model: 128     # Change this HERE to change it everywhere
  d_state: 64
  dropout: 0.1
device: "cpu"
paths:
  processed_data: "data/processed/synth_train.json"
  outputs: "outputs"
